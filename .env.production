# MongoDB Configuration
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/glucoguide

# Redis Configuration (optional, can be removed if not using Redis)
REDIS_HOST=redis-host
REDIS_PORT=6379

# Server Configuration
PORT=4000
NODE_ENV=production

# Security
JWT_SECRET=your-production-jwt-secret-here

# Frontend URL
FRONTEND_URL=https://your-frontend-domain.com

# VAPID keys for push notifications (generate new ones for production)
VAPID_PUBLIC_KEY=your-production-vapid-public-key
VAPID_PRIVATE_KEY=your-production-vapid-private-key