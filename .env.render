# Variables de entorno para Render.com

# MongoDB - Necesitarás crear una base de datos en MongoDB Atlas
MONGO_URI=

# Puerto (Render lo proporciona automáticamente)
PORT=4000

# JWT Secret (debe ser una cadena segura y única)
JWT_SECRET=

# Entorno
NODE_ENV=production

# URL del frontend (se actualizará después del despliegue)
FRONTEND_URL=