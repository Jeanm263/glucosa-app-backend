services:
  mongo:
    image: mongo:6
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  backend:
    build: .
    env_file: .env
    ports:
      - "4000:4000"
    depends_on:
      - mongo
    restart: unless-stopped

volumes:
  mongo_data:
